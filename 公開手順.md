# カラオケビンゴ を GitHub Pages で公開する

所要時間 約5分。git コマンドは使いません（すべてブラウザ上の操作）。
公開後、相手は **URLを開くだけ**。アカウントもアプリのインストールも不要です。

## 用意するファイル

このフォルダの5つ（この手順書は入れなくてOK）。

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体。これ1つで動きます |
| `apple-touch-icon.png` | iPhone でホーム画面に追加したときのアイコン |
| `icon-192.png` / `icon-512.png` | Android・ブラウザ用のアイコン |
| `manifest.webmanifest` | アプリとして起動するための設定 |

## 手順

### 1. GitHub アカウントを作る（持っていれば飛ばす）

https://github.com/signup で無料登録。

### 2. リポジトリを作る

1. https://github.com/new を開く
2. **Repository name** に `karaoke-bingo` と入力
3. **Public** を選ぶ（Private だと無料プランでは公開できません）
4. 「Create repository」

### 3. ファイルをアップロードする

1. 作られたページの **uploading an existing file** のリンクを押す
   （見当たらなければ「Add file」→「Upload files」）
2. 上の5つのファイルをまとめてドラッグ＆ドロップ
3. 下の緑のボタン **Commit changes**

> フォルダごとではなく、**ファイル5つを直接**入れてください。
> `index.html` がいちばん上の階層にある必要があります。

### 4. Pages を有効にする

1. リポジトリの **Settings**（上のタブ）→ 左メニューの **Pages**
2. **Source** を `Deploy from a branch` に
3. **Branch** を `main` / `(root)` にして **Save**

### 5. 1〜2分待つ

Settings → Pages を再読み込みすると、上部にURLが表示されます。

```
https://<あなたのGitHubユーザー名>.github.io/karaoke-bingo/
```

これが共有するURLです。LINEでもメールでもそのまま貼れます。

## 相手に伝えるとよいこと

- **iPhone**：Safariで開く → 下の共有ボタン → **ホーム画面に追加**
  アプリのアイコンとして並び、次からはアドレスバーなしの全画面で起動します
- **進行状況は各自のスマホに保存されます**。同じURLでも人それぞれ別のカードになります
- 音が出ます。うるさいときは画面右上のスピーカーボタンで消せます

## あとから修正したいとき

GitHub 上で `index.html` を開き、鉛筆アイコンで編集して Commit するだけ。
1〜2分で全員に反映されます（URLは変わりません）。

## 注意

Public リポジトリなので、中身（HTMLのソース）は誰でも見られます。
このアプリは個人情報を一切扱っていないので問題ありませんが、
パスワードなどを書き込まないでください。
